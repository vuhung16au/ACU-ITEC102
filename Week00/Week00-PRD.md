Create docs for week 00 

- `00.Python.md`: How to install Python and set up a development environment.
- `01.GitHub.md`: How to install Git and GitHub and set up a repository. Introduce basic Git commands and `gh` CLI.
- `02.VSCode.md`: How to install VS Code and set up a development environment. How to install Jupyter Notebook and Jupyter Lab extensions in VS Code. How to install and set up Python extension in VS Code. How to install and set up Git extension in VS Code. 
- `03.Python-venv.md`: How to create and manage Python virtual environments.
- `04.Python-Libraries.md`: How to install and manage Python libraries using a) `pip` , b) `uv`. In this doc, also introduce `requirements.txt` and `pyproject.toml` for dependency management. In this course: we use: pandas, numpy, matplotlib, seaborn, scikit-learn. Add a note that `uv` is a good alternative to `pip` for managing Python libraries.
- `05.docker.md`: How to install Docker and set up a development environment. a) docker + python using `docker-compose`. How to "deploy" and run a Python application in a Docker container. b) docker + jupyter notebook using `docker-compose`. How to "deploy" and run a Jupyter Notebook in a Docker container.
- `06.Google-Colab.md`: How to use Google Colab for Python development and collaboration. Also write a note that `Kaggle` is a good alternative to Colab for data science projects. Write a section to say that it is recommended to store Jupyter Notebooks in GitHub PRIVATE repositories for version control and collaboration and open the notebook in Colab for execution.

Supported OS: 
- Windows
- macOS
- Linux

Notes: 
- We use, and recommend Python 3.14
- For each docs, add a "verification" section at the end to ensure that the installation and setup were successful.
- Make the docs short and concise, focusing on the essential steps for installation and setup. 