[project]
name = "ml-model-api"
version = "0.1.0"
description = "REST API serving a Machine Learning model for Green Vehicle CO2 Emissions."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "scikit-learn>=1.4.0",
    "pandas>=2.2.0",
    "pydantic>=2.6.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["app"]
