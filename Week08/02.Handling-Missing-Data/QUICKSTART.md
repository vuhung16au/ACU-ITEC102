# Quickstart: Handling Missing Data

## Prerequisites
- `uv` package manager installed.

## Running the Notebook
1. Open your terminal in this directory.
2. Run `make run` to start the Jupyter Notebook environment.
3. Open the `.ipynb` file in the `notebooks/` directory.

## Cleanup
Run `make clean` to remove generated environment files and checkpoints.
